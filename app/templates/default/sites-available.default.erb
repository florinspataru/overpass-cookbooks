server {
        listen 80;
        server_name "~^(www\.)?<%= node['static']['regex_servername'] %>$";
        root <%= node['static']['base_dir'] %>;
        index index.php index.html index.htm;

  	location / {	
  	}
}
